package parser

import (
	"testing"
)

func TestSqlParser(t *testing.T) {
	//	query, err := sqlparser.Parse("SELECT a, b, c FROM 'd' WHERE e = '1' AND f > '2'")
	// query, err := sqlparser.Parse("UPDATE table1 SET b = 2, c = 'df' WHERE e = 1 AND f = 'fdfdf'")
	// assert.Equal(t, nil, err)
	// t.Logf("%+#v", query)
}
